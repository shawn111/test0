- devel:
    no setup
    image_name = images/escore
    os_variant = escore
    unattended_install, check_block_size.4096_512, check_block_size.512_512, svirt_install:
        unattended_file = unattended/escore.ks
        syslog_server_proto = udp
