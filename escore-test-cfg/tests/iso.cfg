# test install from iso
#  - generate qemu image for other tests
    unattended_install:
        unattended_file = unattended/escore-devel.ks
        # cdrom_unattended = isos/isolinux/ks.iso
        kernel = isos/isolinux/vmlinuz
        initrd = isos/isolinux/initrd.img
    unattended_install.cdrom:
        cdrom_cd1 = isos/linux/escore.iso
